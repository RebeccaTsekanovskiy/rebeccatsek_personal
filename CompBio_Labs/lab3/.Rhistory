setwd("C:/Users/Rebecca Tsekanovskiy/Dropbox/PC/Desktop/CompBio_Labs/lab3")
head(dat)
setwd("C:/Users/Rebecca Tsekanovskiy/Dropbox/PC/Desktop/CompBio_Labs/lab3")
# check working directory!
# read in your data set as a csv
# this is an example data set you need to create your own spread sheet from the
# student data sheet and load that in instead!
dat <- read.csv("student_data_sheet.csv")
library(dplyr)
# this is code for you
library(ggplot2)
library(dplyr)
# check working directory!
# read in your data set as a csv
# this is an example data set you need to create your own spread sheet from the
# student data sheet and load that in instead!
dat <- read.csv("student_data_sheet.csv")
head(dat)
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")
plot(col="red")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location",color="red")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")+
geom_point(color="blue")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+geom_point(color="blue")+
# change x axis label
xlab("Site Location")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence), size=5)+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="red"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")
head("name")
ggtile("hi")
ggtitle("hi")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")+
ggtitle("Your Plot Title Here")
ggtitle("Dog)
ggtitle("Dog)
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location")+
ggtitle("Dog")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location (base pairs")+
ggtitle("Dog cpg island locations ")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location (base pairs")+
ggtitle("Dog CpG island locations ")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location (base pairs)")+
ggtitle("Dog CpG island locations ")
# this is tricky to figure out so here is the bare bones code go ahead and modify it make it look nice for your data
# this 'maps' the location for each sequence
# first idnetify data
ggplot(data = dat)+
# next we want ot make a segment from the start to the end wiht the sequnce # being the y axis
geom_segment(aes(x = start_site, y = Sequence, xend = end_site, yend = Sequence, color="purple"), size=5)+
scale_color_identity()+
# just make a nice theme
theme_classic()+
# change x axis label
xlab("Site Location (base pairs)")+
ggtitle("Dog CpG Island Locations ")
# length box plot
ggplot(data = dat, aes(y = GC_content)) +
geom_boxplot() +
theme_classic() +
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content,color="green")) +
geom_boxplot() +
theme_classic() +
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content),color="green") +
geom_boxplot() +
theme_classic() +
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content),color="green") +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content, color="green")) +
geom_boxplot() +
theme_classic() +
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content, color="green"),fill="Blue") +
geom_boxplot() +
theme_classic() +
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content,color="blue")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content,color="blue",fill="green")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("GC Content") +
xlab("")
ggplot(data = dat, aes(y = GC_content,color="blue")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("GC Content") +
xlab("")
# length box plot
ggplot(data = dat, aes(y = length,color="blue")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("GC Content") +
xlab("")
# length box plot
ggplot(data = dat, aes(y = length,color="blue")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("Length") +
xlab("")
# length box plot
ggplot(data = dat, aes(y = length,color="orange")) +
geom_boxplot() +
theme_classic() +
scale_color_identity()+
ylab("Length") +
xlab("")
